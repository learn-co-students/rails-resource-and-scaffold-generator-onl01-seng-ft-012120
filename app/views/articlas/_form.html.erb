<%= form_for(articla) do |f| %>
  <% if articla.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(articla.errors.count, "error") %> prohibited this articla from being saved:</h2>

      <ul>
      <% articla.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :body %>
    <%= f.text_area :body %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
